---
title: "Claude Code Subagents：5 個 parallel agent 同時做嘢（refactor + test + 寫文檔）"
slug: "claude-code-subagents-claude"
issue: 86
publishedAt: 2026-05-24
audience: ["創作者"]
category: "code"
section: "科技"
subcategory: "Subagents"
heroImage: "/hero/claude-code-subagents-claude.webp"
difficulty: 3
timeMinutes: 30
toolsNeeded: ["Claude Code 安裝咗"]
description: "Review 大 PR 30 分鐘順序做。用 subagent 並行：code review + test 覆蓋 + 保安掃描 + PR 描述 4 個 agent 並行 6-8 分鐘搞掂。教你 setup + 邊類任務適合並行。"
keywords: ["Claude Code subagents", "並行 agents", "Claude agent SDK", "AI agent 編排", "Claude Code workflow"]
related: ["claude-code-install-claude", "claude-code-slash-commands-claude", "claude-code-worktrees-claude"]
featured: false
---

## 情境

你係 senior dev。同事 push 咗一個大 PR —— 500 行改動跨 12 個檔案。

你 review 動作：

1. 讀全部檔案 diff：15 分鐘
2. Check test 覆蓋：5 分鐘
3. 手動跑保安掃描：5 分鐘
4. 寫 thoughtful PR comment：10 分鐘

總共：35 分鐘順序做。

呢個順序嘅根源：**任務之間唔互相 dependent** —— code review 唔等 test 覆蓋結果，test 覆蓋唔等保安掃描。但你預設順序跑，因為個腦每次只可以集中 1 個任務。

**Subagents** 解決呢個：一個主 session 同時 spawn N 個 subagent，每個專注唔同子任務，並行跑。

**概念**：
- **主 agent**（你）：編排 + 整合結果
- **Subagents**：並行工人，每個收特定任務 + 返結果

呢篇拆 subagent setup + 5 個每日 use case。

⚠️ Subagent 難度較高 —— 適合已用 Claude Code 1-2 個月 + 熟設定檔嘅人。

## 跟住做

### 1. Subagent 概念清晰啲

```
主 session (你 in interactive Claude Code)
  │
  ├── spawn → Subagent 1 (Code reviewer)
  │             └── output: 5 issue spotted
  │
  ├── spawn → Subagent 2 (Test coverage analyst)
  │             └── output: 80% coverage, 3 untested function
  │
  ├── spawn → Subagent 3 (Security scanner)
  │             └── output: no secret leaked, 1 SQL injection risk
  │
  └── integrate → final summary 畀你
```

每個 subagent 獨立 context window，自己嘅對話歷史。Parallelism = 真實 wall-clock 時間慳到。

### 2. 內建 vs 自訂 subagents

Claude Code 自帶幾種標準 subagent 類型：

- **General-purpose**：開放式任務
- **Explore**：唯讀搜尋整個 codebase
- **Plan**：軟件架構師角色嘅規劃
- **Code reviewer**：專注程式碼品質問題

自訂 subagents：你可以自己 define（下一節）。

### 3. 叫起內建 subagent

喺 Claude Code 對話入面，你可以透過 Agent 工具 spawn subagent：

```
Agent(
  description: "Find all auth-related code paths",
  subagent_type: "Explore",
  prompt: "Search the codebase for all files involved in authentication. Look for auth middleware, login routes, session management. Report file paths + brief description of each. Don't read full files, just locate."
)
```

（具體 syntax 視乎 Claude Code 版本 + IDE 整合。`claude --help` 顯示當前。）

呢個 spawn 一個 Explore subagent。佢返一份摘要，主 agent 接住處理完整檔案。

### 4. Spawn 多個 subagents 並行

關鍵 insight：你喺同一個回應 spawn 多個 subagents，即係並行。

例（PR review）：

```
我哋 spawn 4 個 subagents parallel:

Agent({
  description: "Code quality review",
  subagent_type: "general-purpose",
  prompt: "Review the PR diff in current branch. Focus on: error handling, naming, complexity. Output 3-5 specific issues with file:line references."
})

Agent({
  description: "Test coverage audit",
  subagent_type: "general-purpose",
  prompt: "Identify which functions in the PR diff lack test coverage. Check /tests folder. Output: list of untested function names + suggested test scenarios."
})

Agent({
  description: "Security risk scan",
  subagent_type: "general-purpose",
  prompt: "Scan PR diff for security risks: SQL injection, XSS, exposed secrets, unsafe deserialization. Output: risk level + specific code references."
})

Agent({
  description: "Generate PR description",
  subagent_type: "general-purpose",
  prompt: "Read the PR diff and write a 200-word PR description: what changed, why, test plan. Output in markdown."
})
```

4 個 agent 同時 spawn → 並行做 → 主 session 等 4 個都搞掂 → 整合 4 個結果 → 你睇到全面 review。

Wall-clock 時間：6-8 分鐘 vs 順序 30-35 分鐘。

### 5. 自訂 subagent 定義

定義可重用 subagent 喺 `~/.claude/agents/`（user-level）或者 `./.claude/agents/`（project-level）：

檔案 `code-reviewer.md`：

```markdown
---
name: code-reviewer
description: Specialist for thorough code review. Use proactively after writing significant new code or before commit.
tools: Read, Grep, Bash(git diff:*)
---

You are a senior code reviewer with 15 years experience in JavaScript/TypeScript and Python.

Your job:
1. Read the recent code changes carefully (use `git diff` to see)
2. Identify specific issues in these categories:
   - Bugs / edge case missing
   - Performance concerns
   - Naming / readability
   - Error handling gaps
   - Security issue
3. Output: 5-10 specific findings, each with:
   - File path + line number
   - Issue type
   - 1 sentence explanation
   - Suggested fix

Tone: Direct, specific. No filler. Treat me as peer dev.
```

存。Claude Code 開新 session 時自動 load。

用：
```
Agent({
  description: "Review my changes",
  subagent_type: "code-reviewer",
  prompt: "Review the last 3 commits in current branch"
})
```

### 6. 每日 subagent 用途

✅ **用途 1：並行調查**（你讀大型 codebase）

```
Agent({ subagent_type: "Explore", prompt: "Find database schema files" })
Agent({ subagent_type: "Explore", prompt: "Find auth middleware" })
Agent({ subagent_type: "Explore", prompt: "Find API route definitions" })
```

3 個並行調查，主 session 整合結果，你決定喺邊度集中。

✅ **用途 2：多角度 review**

```
Agent({ subagent_type: "security-reviewer", prompt: "Audit auth flow security" })
Agent({ subagent_type: "performance-reviewer", prompt: "Audit auth flow perf" })
Agent({ subagent_type: "readability-reviewer", prompt: "Audit auth flow clarity" })
```

3 個唔同角度，同一段 code。主 session 綜合。

✅ **用途 3：並行內容生成**

```
Agent({ prompt: "Draft user-facing release notes for these commits" })
Agent({ prompt: "Draft internal team update for these commits" })
Agent({ prompt: "Draft social media announcement for these commits" })
```

3 個受眾，3 種聲線，並行。

✅ **用途 4：Deploy 前並行檢查**

```
Agent({ prompt: "Run npm run test and report failures" })
Agent({ prompt: "Run npm run lint and report errors" })
Agent({ prompt: "Check for hardcoded secrets in diff" })
Agent({ prompt: "Verify all new env vars documented in README" })
```

4 個 deploy 關卡檢查，並行。1 個 fail → 停 deploy。

✅ **用途 5：Bug 調查**

```
Agent({ prompt: "Search for error 'XYZ' in logs" })
Agent({ prompt: "Find similar past issue in GitHub" })
Agent({ prompt: "Check related test cases for coverage gap" })
```

3 個並行調查角度，主 agent 整合發現。

## 變化

### 變化 1：幾時唔好用 subagents

唔適合並行：

❌ 任務之間嚴格依賴（每個都依賴上一個 output）
❌ 單檔案編輯（並行冇 gain）
❌ 簡單查詢（spawn 嘅 overhead > 任務時間）
❌ 有狀態嘅任務序列（需要共用 context）

✅ 適合：
- 獨立調查
- 多角度 review
- 並行內容生成
- Deploy 前多項檢查
- 多來源資料抓取

Subagent 嘅價值同任務獨立性成正比。

### 變化 2：Sub-subagents（遞迴）

進階：subagent 自己可以 spawn sub-subagents。

例如「Refactor 架構」主 agent → spawn「Plan refactor」subagent → 嗰個再 spawn「Read existing auth module」sub-subagent。

3 層通常夠。再深 = 編排複雜度過高。

### 變化 3：Subagent + Skills + Commands 三者組合

Skills 層（按第 79 篇）負責「特定程序邏輯」。Subagent 負責「並行」。

例：
- `/pre-deploy-check` slash command 觸發
- 入面 spawn 4 個 subagent（test / lint / security / 文檔）
- 每個 subagent 入面可以叫起 skills（例如 security-audit skill）
- 整合後出單一摘要報告

呢個 3 層組合（command → subagent → skill）係進階 workflow 架構。

## 一個心態

Subagent 嘅深層 insight：**CPU / IO 並行對伺服器任務已成熟，但認知任務嘅並行對人類仍然困難。AI subagent 係突破**。

之前你個腦 1 條 thread 順序行。即使多監察都唔可以真正並行注意力。Subagent = 將並行外判 —— 5 個 subagent 同時跑 5 個任務，你主腦只負責編排 + 整合。

呢個 paradigm 顯著改變 senior dev / 編排者嘅角色 —— 唔再係「單獨執行」，而係「多 agent 編排」。

最後提醒：

- ✅ **Subagent 嘅價值喺「獨立任務嘅並行」**。識別獨立任務係關鍵技能。
- ⚠️ **Subagent 之間唔共用 context**。各自 fresh 記憶。Spawn 時必須包含所有需要嘅 context。
- 🎯 **自訂 subagent 1 個月會自然進化**。你每日出嘅時候會發覺「I keep doing same review pattern」，codify 入可重用嘅 subagent 定義。

下個大 PR review 試 4 個並行 subagent。30 分鐘 → 8 分鐘。你個腦會嘆「點解之前順序咁慢」。
